{
  "name": "code-dao-vendor",
  "private": true,
  "description": "Solidity dependencies vendored via npm so the build needs no git submodules. The devDependencies are not compiled: they are the sources the LayerZero and Stargate interfaces in src/interfaces/ILayerZero.sol were transcribed from, pinned so a reviewer can diff.",
  "dependencies": {
    "@openzeppelin/contracts": "5.1.0",
    "@uniswap/v2-core": "1.0.1",
    "@uniswap/v2-periphery": "1.1.0-beta.0",
    "@chainlink/contracts": "1.5.0",
    "ds-test": "github:dapphub/ds-test",
    "forge-std": "1.1.2",
    "solady": "0.1.26"
  },
  "devDependencies": {
    "@layerzerolabs/lz-evm-protocol-v2": "2.0.11",
    "@layerzerolabs/lz-evm-oapp-v2": "2.3.44",
    "@stargatefinance/stg-evm-v2": "9.0.0"
  }
}

# CODE DAO interface

Green-black terminal surface. Marketing at `/`. The desk at `/app` (stake, proposals, vintages)
mirrors the CULT app split, and stays offline until genesis.

```
npm install
npm run dev
```

Open `http://localhost:3000`.
